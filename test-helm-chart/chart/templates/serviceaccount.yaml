{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccount.name | default (include "app.fullname" .) }}
  labels:
    {{- include "app.labels" . | nindent 4 }}
{{- end }}
