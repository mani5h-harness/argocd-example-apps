apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "app.fullname" . }}-extra-config-120
  labels:
    {{- include "app.labels" . | nindent 4 }}
data:
  CONFIG_KEY_120: "value-120"
  BATCH_ID: "120"
  TIMESTAMP: "2026-03-20"
  DESCRIPTION: "Extra configuration map number 120 for load testing"
