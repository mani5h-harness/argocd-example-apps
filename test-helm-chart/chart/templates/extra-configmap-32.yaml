apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "app.fullname" . }}-extra-config-32
  labels:
    {{- include "app.labels" . | nindent 4 }}
data:
  CONFIG_KEY_32: "value-32"
  BATCH_ID: "32"
  TIMESTAMP: "2026-03-20"
  DESCRIPTION: "Extra configuration map number 32 for load testing"
