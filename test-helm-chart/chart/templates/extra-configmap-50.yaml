apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "app.fullname" . }}-extra-config-50
  labels:
    {{- include "app.labels" . | nindent 4 }}
data:
  CONFIG_KEY_50: "value-50"
  BATCH_ID: "50"
  TIMESTAMP: "2026-03-20"
  DESCRIPTION: "Extra configuration map number 50 for load testing"
