apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "app.fullname" . }}-extra-config-60
  labels:
    {{- include "app.labels" . | nindent 4 }}
data:
  CONFIG_KEY_60: "value-60"
  BATCH_ID: "60"
  TIMESTAMP: "2026-03-20"
  DESCRIPTION: "Extra configuration map number 60 for load testing"
