apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "app.fullname" . }}-extra-config-64
  labels:
    {{- include "app.labels" . | nindent 4 }}
data:
  CONFIG_KEY_64: "value-64"
  BATCH_ID: "64"
  TIMESTAMP: "2026-03-20"
  DESCRIPTION: "Extra configuration map number 64 for load testing"
