apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "app.fullname" . }}-extra-config-1
  labels:
    {{- include "app.labels" . | nindent 4 }}
data:
  CONFIG_KEY_1: "value-1"
  BATCH_ID: "1"
  TIMESTAMP: "2026-03-20"
  DESCRIPTION: "Extra configuration map number 1 for load testing"
