apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "app.fullname" . }}-extra-config-80
  labels:
    {{- include "app.labels" . | nindent 4 }}
data:
  CONFIG_KEY_80: "value-80"
  BATCH_ID: "80"
  TIMESTAMP: "2026-03-20"
  DESCRIPTION: "Extra configuration map number 80 for load testing"
