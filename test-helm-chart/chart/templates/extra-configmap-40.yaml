apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "app.fullname" . }}-extra-config-40
  labels:
    {{- include "app.labels" . | nindent 4 }}
data:
  CONFIG_KEY_40: "value-40"
  BATCH_ID: "40"
  TIMESTAMP: "2026-03-20"
  DESCRIPTION: "Extra configuration map number 40 for load testing"
