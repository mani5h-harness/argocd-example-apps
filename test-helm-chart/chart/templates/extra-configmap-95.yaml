apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "app.fullname" . }}-extra-config-95
  labels:
    {{- include "app.labels" . | nindent 4 }}
data:
  CONFIG_KEY_95: "value-95"
  BATCH_ID: "95"
  TIMESTAMP: "2026-03-20"
  DESCRIPTION: "Extra configuration map number 95 for load testing"
