apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "app.fullname" . }}-extra-config-100
  labels:
    {{- include "app.labels" . | nindent 4 }}
data:
  CONFIG_KEY_100: "value-100"
  BATCH_ID: "100"
  TIMESTAMP: "2026-03-20"
  DESCRIPTION: "Extra configuration map number 100 for load testing"
