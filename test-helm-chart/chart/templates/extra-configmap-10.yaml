apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "app.fullname" . }}-extra-config-10
  labels:
    {{- include "app.labels" . | nindent 4 }}
data:
  CONFIG_KEY_10: "value-10"
  BATCH_ID: "10"
  TIMESTAMP: "2026-03-20"
  DESCRIPTION: "Extra configuration map number 10 for load testing"
